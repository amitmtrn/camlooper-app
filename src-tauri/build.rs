fn main() {
    // The Windows virtual camera now uses the bundled softcam DirectShow driver
    // (built in CI, registered by the installer) instead of an in-tree custom filter,
    // so there is no native C++ to compile here anymore.
}
